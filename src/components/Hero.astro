---
import "../styles/global.css";
---

<section class="relative min-h-screen bg-white dark:bg-gray-900 flex flex-col">
  <div
    class="flex-1 flex flex-col justify-center items-center px-4 sm:px-6 lg:px-8 pt-20 pb-8"
  >
    <div class="max-w-6xl mx-auto text-center space-y-6 sm:space-y-8">
      <h1
        class="text-4xl sm:text-5xl md:text-6xl lg:text-7xl xl:text-8xl font-bold text-gray-900 dark:text-white leading-tight"
      >
        Google Developers Group
      </h1>

      <div class="max-w-4xl mx-auto space-y-4 sm:space-y-6">
        <p
          class="text-base sm:text-lg md:text-xl lg:text-2xl text-gray-700 dark:text-gray-300 leading-relaxed"
        >
          Join a Google Developer Group to build new skills and connect with
          local developers, online and in person.
        </p>

        <p
          class="text-base sm:text-lg md:text-xl lg:text-2xl text-gray-700 dark:text-gray-300 leading-relaxed"
        >
          Whether you're a beginner or a pro, all interested in technology are
          welcome in this inclusive community.
        </p>
      </div>

      <div class="pt-4 sm:pt-6">
        <button
          class="bg-blue-500 hover:bg-blue-600 text-white font-semibold px-8 py-3 sm:px-10 sm:py-4 rounded-xl text-base sm:text-lg transition-all duration-300 hover:scale-105 shadow-lg"
        >
          Get Started
        </button>
      </div>
    </div>
  </div>

  <div class="waves">
    <svg
      class="waves"
      xmlns="http://www.w3.org/2000/svg"
      xmlns:xlink="http://www.w3.org/1999/xlink"
      viewBox="0 24 150 28"
      preserveAspectRatio="none"
      shape-rendering="auto"
    >
      <defs>
        <path
          id="gentle-wave"
          d="M-160 44c30 0 58-18 88-18s 58 18 88 18 58-18 88-18 58 18 88 18 v44h-352z"
        ></path>
      </defs>
      <g class="parallax">
        <use xlink:href="#gentle-wave" x="48" y="0" fill="rgba(195,236,246,0.7)"
        ></use>
        <use xlink:href="#gentle-wave" x="48" y="3" fill="rgba(195,236,246,0.5)"
        ></use>
        <use xlink:href="#gentle-wave" x="48" y="5" fill="rgba(195,236,246,0.3)"
        ></use>
        <use xlink:href="#gentle-wave" x="48" y="7" fill="#c3ecf6"></use>
      </g>
    </svg>
  </div>
</section>

<style>
  .waves {
    position: relative;
    width: 100%;
    height: 15vh;
    margin-bottom: -7px;
    min-height: 100px;
    max-height: 200px;
  }

  .parallax > use {
    animation: move-forever 25s cubic-bezier(0.55, 0.5, 0.45, 0.5) infinite;
  }

  .parallax > use:nth-child(1) {
    animation-delay: -2s;
    animation-duration: 7s;
  }

  .parallax > use:nth-child(2) {
    animation-delay: -3s;
    animation-duration: 10s;
  }

  .parallax > use:nth-child(3) {
    animation-delay: -4s;
    animation-duration: 13s;
  }

  .parallax > use:nth-child(4) {
    animation-delay: -5s;
    animation-duration: 20s;
  }

  @keyframes move-forever {
    0% {
      transform: translate3d(-90px, 0, 0);
    }
    100% {
      transform: translate3d(85px, 0, 0);
    }
  }

  @media (max-width: 640px) {
    .waves {
      height: 12vh;
      min-height: 80px;
    }
  }
</style>
