---
import Layout from "../layouts/Layout.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import blogs from "../content/blogs.json";
import "../styles/global.css";

const colorConfig = {
  blue: { border: "border-t-blue-500", text: "text-blue-600" },
  green: { border: "border-t-green-500", text: "text-green-600" },
  red: { border: "border-t-red-500", text: "text-red-600" },
  yellow: { border: "border-t-amber-500", text: "text-amber-600" },
};

type Color = keyof typeof colorConfig;
const colorCycle = Object.keys(colorConfig) as Color[];
const getColorForIndex = (index: number): Color => {
  return colorCycle[index % colorCycle.length];
};
---

<Layout>
  <head>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;700&family=Inter:wght@400;500&display=swap"
      rel="stylesheet"
    />
  </head>

  <Header />

  <section class="bg-gray-50 dark:bg-gray-900 py-20 min-h-screen">
    <div class="container mx-auto px-4">
      <header class="text-center mb-16">
        <h1
          class="font-fira-code text-4xl md:text-5xl font-bold text-gray-800 dark:text-gray-100"
        >
          <span class="text-blue-600">&lt;</span> BLOGS <span
            class="text-green-600">/</span
          >
          <span class="text-red-600">&gt;</span>
        </h1>
        <p
          class="font-jetbrains-mono text-gray-500 dark:text-gray-400 mt-3 text-sm"
        >
          // All our thoughts and discoveries
        </p>
      </header>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        {
          blogs.map((blog, index) => {
            const colorName = getColorForIndex(index);
            const { border, text } = colorConfig[colorName];
            return (
              <div
                class={`group bg-gray-100 dark:bg-gray-800 rounded-lg overflow-hidden border-t-4 ${border} flex flex-col hover:shadow-lg transition`}
              >
                <a
                  href={blog.link}
                  target="_blank"
                  rel="noopener noreferrer"
                  class="flex flex-col flex-grow"
                >
                  <img
                    src={blog.img}
                    alt={blog.title}
                    class="w-full h-48 object-cover"
                    loading="lazy"
                  />
                  <div class="p-5 flex flex-col flex-grow">
                    <h2 class="font-jetbrains-mono text-2xl font-bold text-gray-900 dark:text-gray-100 mb-3">
                      {blog.title}
                    </h2>
                    <p class="text-gray-700 dark:text-gray-300 leading-relaxed text-sm mb-6 flex-grow">
                      {blog.excerpt}
                    </p>
                    <div class="flex justify-between items-center mt-auto">
                      <p class="font-jetbrains-mono text-xs text-gray-400 dark:text-gray-500">
                        // by {blog.author}
                      </p>
                      <span class={`font-jetbrains-mono text-sm ${text}`}>
                        View &rarr;
                      </span>
                    </div>
                  </div>
                </a>
              </div>
            );
          })
        }
      </div>
    </div>
  </section>

  <Footer />
</Layout>

<style>
  body {
    font-family: "Inter", sans-serif;
  }
  .font-jetbrains-mono {
    font-family: "JetBrains Mono", monospace;
  }
</style>
